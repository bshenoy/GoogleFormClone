import React from 'react';
import FormBuilder from '../components/FormBuilder';

const Form = () => (
    <div>
        <h1>Fill the Form</h1>
        <FormBuilder />
    </div>
);

export default Form;
